# Torch is installed from the CPU-only wheel index in the Dockerfile.
# Installing it from PyPI instead pulls the CUDA build (~2.5 GB).
torch==2.12.1
torchvision==0.27.1

ultralytics==8.3.31
opencv-python==4.10.0.84
numpy==2.1.3
pillow==11.0.0

flask==3.1.3
flask-cors==6.0.5
gunicorn==26.0.0
